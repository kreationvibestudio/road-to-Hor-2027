// Admin page password. Copy this file to admin-config.js and set your own password.
// Do not commit admin-config.js (it is in .gitignore).
// Then open your site at /admin.html and enter the password to view constituency requests.

window.ADMIN_PASSWORD = 'change-me';
